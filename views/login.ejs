<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login to Scatch | Access Your Account</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom font application */
        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
</head>

<body class="min-h-screen bg-gray-50 flex flex-col">

    <nav class="w-full flex justify-between items-center px-6 md:px-12 py-4 shadow-md bg-white sticky top-0 z-10">
        <a href="/" class="text-3xl font-extrabold text-blue-700 tracking-tight hover:text-blue-800 transition duration-200" aria-label="Scatch Home">
            Scatch
        </a>

        <!-- <div class="flex gap-4 md:gap-6 items-center">
            <a href="/register" 
                class="px-4 py-2 bg-blue-600 text-white rounded-lg text-sm font-semibold shadow-md hover:bg-blue-700 transition duration-200 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50">
                Create Account
            </a>
        </div> -->
    </nav>

    <div class="flex-grow flex items-center justify-center p-4">
        <div class="bg-white shadow-2xl rounded-xl p-8 sm:p-10 w-full max-w-md border border-gray-200">
            <h2 class="text-4xl font-extrabold text-gray-900 text-center mb-2">Welcome Back</h2>
            <p class="text-gray-500 text-center mb-8 text-lg">Sign in to continue your journey.</p>

            <!-- Success Message -->
            <% if (success && success.length > 0) { %>
                <div class="mb-6 p-4 bg-green-50 border border-green-200 rounded-lg">
                    <div class="flex items-start">
                        <svg class="w-5 h-5 text-green-600 mt-0.5 mr-3 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
                        </svg>
                        <p class="text-sm font-medium text-green-800"><%= success %></p>
                    </div>
                </div>
            <% } %>

            <!-- Error Message -->
            <% if (error && error.length > 0) { %>
                <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-lg">
                    <div class="flex items-start">
                        <svg class="w-5 h-5 text-red-600 mt-0.5 mr-3 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
                        </svg>
                        <p class="text-sm font-medium text-red-800"><%= error %></p>
                    </div>
                </div>
            <% } %>

            <form action="/users/login" method="POST" class="space-y-6">

                <div>
                    <label for="email" class="block text-sm font-semibold text-gray-700 mb-2">Email Address</label>
                    <input type="email" id="email" name="email" required
                        placeholder="you@example.com"
                        class="w-full px-5 py-3 bg-gray-50 border border-gray-300 rounded-lg placeholder-gray-400 text-gray-900 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-150">
                </div>

                
                <div class="relative">
                    <div class="flex justify-between items-center mb-2">
                        <label for="passwordInput" class="text-sm font-semibold text-gray-700">Password</label>
                        <a href="/forgot" class="text-sm text-blue-600 hover:text-blue-700 hover:underline transition">Forgot Password?</a>
                    </div>
                    
                    <input type="password" id="passwordInput" name="password" required
                        placeholder="Enter your password"
                        class="w-full px-5 py-3 bg-gray-50 border border-gray-300 rounded-lg placeholder-gray-400 text-gray-900 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition duration-150 pr-12">

                    <button type="button" id="togglePassword"
                        class="absolute inset-y-0 right-0 top-0 mt-8 flex items-center pr-3 text-gray-400 hover:text-gray-600 focus:outline-none"
                        aria-label="Toggle password visibility">
                        
                        <svg id="eyeOpen" xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.477 0 8.268 2.943 9.542 7-1.274 4.057-5.065 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                        </svg>

                        <svg id="eyeClosed" xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.875 18.825A10.05 10.05 0 0112 19c-4.477 0-8.268-2.943-9.542-7a9.97 9.97 0 012.727-4.568M9.88 9.88a3 3 0 104.24 4.24" />
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3l18 18" />
                        </svg>
                    </button>
                </div>


                <button type="submit"
                    class="w-full py-3 mt-8 bg-blue-600 text-white rounded-xl text-lg font-bold shadow-lg shadow-blue-500/50 hover:bg-blue-700 transition duration-200 focus:outline-none focus:ring-4 focus:ring-blue-500 focus:ring-opacity-50 active:bg-blue-800">
                    Sign In
                </button>
            </form>

            <p class="text-center text-gray-500 mt-6 text-sm">
                Don't have an account?
                <a href="/register" class="text-blue-600 font-semibold hover:text-blue-700 hover:underline transition duration-150">Create one</a>
            </p>
        </div>
    </div>

    <footer class="w-full text-center py-5 text-gray-500 text-sm border-t border-gray-200">
        © <span id="year"></span> Scatch · All Rights Reserved
    </footer>

    <script>
        document.getElementById('year').innerText = new Date().getFullYear();

        // Refined JavaScript for the Show/Hide Password feature
        const passwordInput = document.getElementById("passwordInput");
        const togglePassword = document.getElementById("togglePassword");
        const eyeOpen = document.getElementById("eyeOpen");
        const eyeClosed = document.getElementById("eyeClosed");

        togglePassword.addEventListener("click", () => {
            const isPassword = passwordInput.type === "password";
            
            // Toggle the type attribute
            passwordInput.type = isPassword ? "text" : "password";

            // Toggle icons and aria-label
            eyeOpen.classList.toggle("hidden");
            eyeClosed.classList.toggle("hidden");
            togglePassword.setAttribute('aria-label', isPassword ? 'Hide password' : 'Show password');
        });
    </script>
</body>
</html>